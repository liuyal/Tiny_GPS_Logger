package com.example.tiny_gps

import android.app.Application

enum class statusID {
    not_connected, connecting_phase1, connecting_phase2, connected
}

class ApClass(id:String) {

    var id = id

    init{}
}